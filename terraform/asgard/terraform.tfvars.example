# Non-sensitive config — copy to terraform.tfvars and fill in your values.
# Secrets (proxmox_api_token) live in secrets.enc.yaml (gitignored).
# Create with: sops terraform/asgard/secrets.enc.yaml
proxmox_endpoint = "https://<PROXMOX_IP>:8006"
proxmox_node     = "yggdrasil"
datastore_id     = "local-zfs"
iso_file_id      = "local:iso/OPNsense-<VERSION>-dvd-amd64.iso"
vm_id            = 0  # Proxmox VM ID
